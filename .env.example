# =============================================================================
# Godot AI Development Assistant - Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your values

# =============================================================================
# API Provider Configuration
# =============================================================================
# Choose your LLM provider: "anthropic" or "openai"
API_PROVIDER=anthropic

# API Keys - You need at least one of these based on your API_PROVIDER choice
ANTHROPIC_API_KEY=your_anthropic_key_here
OPENAI_API_KEY=your_openai_key_here

# =============================================================================
# Embedding Provider Configuration
# =============================================================================
# Choose embedding provider:
# - "local": Uses sentence-transformers (free, no API key needed, runs in container)
# - "openai": Uses OpenAI embeddings (requires OPENAI_API_KEY and paid tokens)
EMBEDDING_PROVIDER=local

# =============================================================================
# Path Configuration
# =============================================================================
# IMPORTANT: Path to your Godot project (absolute path)
# For WSL users on Windows: Use Linux path format like /mnt/c/Users/username/projects/my_game
# NOT Windows format like C:\Users\...
GODOT_PROJECT_PATH=/home/yourusername/path/to/your/godot/project

# =============================================================================
# LLM Model Configuration (Optional - defaults provided)
# =============================================================================
# Anthropic model to use (default: claude-sonnet-4-20250514)
# ANTHROPIC_MODEL=claude-sonnet-4-20250514

# OpenAI model to use (default: gpt-4-turbo-preview)
# OPENAI_MODEL=gpt-4-turbo-preview

# Temperature for LLM responses (0 = deterministic, 1 = creative)
# LLM_TEMPERATURE=0

# =============================================================================
# RAG Configuration (Optional - defaults provided)
# =============================================================================
# Size of text chunks for document processing
# RAG_CHUNK_SIZE=1000

# Overlap between chunks to preserve context
# RAG_CHUNK_OVERLAP=200

# Number of relevant chunks to retrieve per query
# RAG_RETRIEVAL_K=6

# =============================================================================
# Web Application Configuration (Optional - defaults provided)
# =============================================================================
# Host address for web server
# WEB_HOST=0.0.0.0

# Port for web server
# WEB_PORT=5000

# Enable debug mode (true/false)
# WEB_DEBUG=true

# =============================================================================
# Localization (Optional - defaults provided)
# =============================================================================
# Language for console output
# LANGUAGE=en